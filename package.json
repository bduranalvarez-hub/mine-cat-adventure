{
  "name": "mine-cat-adventure",
  "version": "1.0.0",
  "private": true,
  "description": "Mine Cat Adventure - runner infinito de vagonetas mineras",
  "scripts": {
    "build:mobile": "node scripts/build-mobile.mjs",
    "cap:sync": "npm run build:mobile && npx cap sync",
    "android:open": "npm run cap:sync && npx cap open android",
    "android:build-debug": "npm run cap:sync && cd android && gradlew.bat assembleDebug",
    "android:build-release": "npm run cap:sync && cd android && gradlew.bat bundleRelease"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^7.0.0"
  },
  "dependencies": {
    "@capacitor-community/admob": "^7.2.0",
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/share": "^7.0.4"
  }
}
